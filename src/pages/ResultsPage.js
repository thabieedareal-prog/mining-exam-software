import React from 'react';\nimport '../styles/ResultsPage.css';\n\nfunction ResultsPage({ results, onHome }) {\n  if (!results) return null;\n\n  const passPercentage = 50;\n  const isPassed = results.percentage >= passPercentage;\n\n  return (\n    <div className=\"results-container\">\n      <div className=\"results-card\">\n        <div className={`results-header ${isPassed ? 'pass' : 'fail'}`}>\n          <h1>{isPassed ? '✓ PASSED' : '✗ FAILED'}</h1>\n          <p>{results.candidateName}</p>\n        </div>\n\n        <div className=\"results-stats\">\n          <div className=\"stat-box\">\n            <h3>Score</h3>\n            <p className=\"score\">{results.correctAnswers}/{results.totalQuestions}</p>\n          </div>\n          <div className=\"stat-box\">\n            <h3>Percentage</h3>\n            <p className=\"percentage\">{results.percentage.toFixed(2)}%</p>\n          </div>\n          <div className=\"stat-box\">\n            <h3>Subject</h3>\n            <p>{results.subject}</p>\n          </div>\n          <div className=\"stat-box\">\n            <h3>Time Taken</h3>\n            <p>{Math.floor(results.timeTaken / 60)} min {results.timeTaken % 60} sec</p>\n          </div>\n        </div>\n\n        <div className=\"results-detail\">\n          <h3>Detailed Results</h3>\n          <div className=\"results-list\">\n            {results.results.map((result, idx) => (\n              <div key={idx} className={`result-item ${result.isCorrect ? 'correct' : 'incorrect'}`}>\n                <div className=\"result-question\">\n                  <strong>Q{idx + 1}: {result.question}</strong>\n                </div>\n                <div className=\"result-answer\">\n                  <p><strong>Your Answer:</strong> {result.userAnswer || 'Not answered'}</p>\n                  {!result.isCorrect && (\n                    <p><strong>Correct Answer:</strong> {result.correctAnswer}</p>\n                  )}\n                </div>\n              </div>\n            ))}\n          </div>\n        </div>\n\n        <button className=\"btn btn-primary\" onClick={onHome}>\n          Back to Home\n        </button>\n      </div>\n    </div>\n  );\n}\n\nexport default ResultsPage;\n